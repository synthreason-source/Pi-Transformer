AI systems being "safe", you never know until you try.

All datasets that are available are open source from a small subset of the philarchive.org dataset.

Largely complete code design...

Example command: python "QKD_AI_QRNG.py" --input singlekb.txt --prompt "existence is the key" --qrng --qrng-output rng.json --qrng-reveal-output secret.json --public-log test_public.json --proof-log test_proof.json --transcript-log transcript.json
